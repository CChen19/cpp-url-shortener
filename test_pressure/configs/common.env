# Shared defaults for Phase 0 scenarios (sourced by run_scenarios.sh).
BASE_URL="${BASE_URL:-http://127.0.0.1:9006}"
RATE="${RATE:-200}"
DURATION="${DURATION:-15s}"
SEED_COUNT="${SEED_COUNT:-50}"
ZIPF_S="${ZIPF_S:-1.2}"
WORKERS="${WORKERS:-64}"
TIMEOUT="${TIMEOUT:-2s}"
